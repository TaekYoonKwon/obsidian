From regular [[Reinforcement Learning]], we replace our policy with deep neural network. This reward signal that we get occassionally should somehow strengthen the link between neurons that led to the action in the policy. In terms of [[Q-Learning]], we can replace the Quality function with Quality network. 

**Human-level control through deep reinforcement learning - 2015 Nature**
Transferring learning - General intelligence

Neural network allows us to express complex functions that we weren't able to express using analytical functions before. 